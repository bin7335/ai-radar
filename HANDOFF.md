# AI Radar 핸드오프 (Hand-off) 문서

본 문서는 `ai-radar` 프로젝트의 인수인계 및 중점 검토 사항을 기록한 문서입니다. (작성일: 2026-09-13)

## 1. 현재까지 완료된 작업 내역 (v0.4)

### 🚀 크롤링 파이프라인 (Backend / Python)
- **Hacker News (Algolia API)**: 최신순이 아닌 `Hot(추천순)` 기반으로 AI/Agent 관련 뉴스 7건 크롤링 완료.
- **GitHub Trending (BeautifulSoup)**: 깃허브 실시간 트렌딩 페이지를 스크래핑하여 AI/Agent 관련 오픈소스 레포지토리 최대 5건 크롤링 연동 완료. (합계 12건의 핫이슈 확보)
- **Gemini 모델 마이그레이션**: 구글의 모델 사용 제한 정책(2.5-flash 신규 유저 차단)에 대응하여 코드를 `gemini-3.6-flash`로 하드코딩 업데이트 완료.
- **카테고리 자동 분류**: Gemini 프롬프트에 지시어를 추가하여 요약과 동시에 `[오픈소스, 뉴스, 정보, 커뮤니티]` 중 하나로 기사를 자동 분류하도록 구현.

### 🎨 프론트엔드 디자인 (Frontend / HTML+JS)
- **Anti-Vibe-Coding 원칙 적용**: 답답한 양산형 카드 UI, 드롭 섀도우를 모두 제거하고 미니멀한 리스트/칸반(Kanban) 스타일 적용.
- **4열 다단 레이아웃 (Kanban)**: 화면을 4개의 열(오픈소스 / 뉴스 / 꿀팁정보 / 커뮤니티)로 나누어 한눈에 글로벌 트렌드를 파악할 수 있도록 구현.
- **메타데이터 파싱**: `Hacker News • github.com` 처럼 스크랩 출처와 실제 뉴스 원본의 도메인(hostname)이 병기되도록 JS 파서 구현 완료.

---

## 2. 중점적으로 검토해야 할 사항 (Review Focus)

다음 작업자(또는 선생님 본인)께서 추후 이 프로젝트를 이어받거나 유지보수하실 때 반드시 체크하셔야 할 핵심 포인트입니다.

### ⚠️ A. GitHub Actions (자동화 및 권한)
- **Cron 주기 확인**: `.github/workflows/scraper.yml`에 설정된 주기가 하루 4번 돌고 있는지 파악해야 합니다.
- **데이터 증식 방지**: 현재 `main.py`는 과거 데이터를 덮어쓰거나(insert 0) 누적합니다. 파일 용량이 무한히 커지는 것을 막기 위해 추후 "최대 30일치만 보존"하는 슬라이싱 로직이 정상 작동하는지 확인이 필요합니다.

### ⚠️ B. 구글 Gemini API 한도 및 에러 (매우 중요)
- 현재 12개의 기사를 요약할 때, 구글 무료 API의 분당 호출 제한(Rate Limit)을 피하기 위해 `time.sleep(6)`이 걸려 있습니다.
- 만약 추후 GitHub Actions 로그에 `429 Too Many Requests` 에러가 발생한다면, 이 `sleep` 시간을 더 길게 늘려주셔야 합니다. (방금 전 개발 테스트 중 단기간에 여러번 스크립트를 재실행했더니 Rate Limit 초과 에러가 발생하여 모든 뉴스가 '뉴스' 카테고리에 몰리는 현상이 있었습니다.)
- 또한 구글이 2026년에 1.5, 2.5 모델을 순차적으로 막아버린 전력이 있습니다. 추후 `404 NOT_FOUND` 에러가 뜬다면 `main.py`의 `gemini-3.6-flash` 부분을 그 시점의 최신 모델 이름으로 바꿔주어야 합니다.

### ⚠️ C. GitHub Trending 구조 변경 대응
- `scrape_github_trending()` 함수는 BeautifulSoup을 이용해 HTML의 DOM 구조(`article.Box-row`)를 긁어옵니다.
- 깃허브가 트렌딩 페이지의 UI/HTML 구조를 개편하면 이 함수가 빈 배열 `[]`을 반환할 수 있습니다. 오픈소스 열이 텅 빈다면 이 DOM 셀렉터 로직을 먼저 의심하고 수정해야 합니다.

### ⚠️ D. UI/UX 확장성
- 현재 `index.html`은 CDN을 이용해 TailwindCSS를 불러오고 있습니다.
- 모바일(반응형)에서는 4열이 1열 또는 2열로 자연스럽게 접히도록 `grid-cols-1 md:grid-cols-2 xl:grid-cols-4` 로직을 짜두었으나, 실제 스마트폰에서 텍스트가 잘리는지(truncate) 뷰포트 검토가 필요합니다.

